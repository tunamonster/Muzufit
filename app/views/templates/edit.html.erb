	
	<div class="row">
	<div class="row col s6">
		<%= form_for(@template) do |f| %>
			<%= f.label :title %>
			<%= f.text_field :title %>

			<%= f.label :content %>
			<%= f.text_area :content %>
		<%= f.submit "update template", class: "btn" %>
		<% end %>