<% unless current_user == @user %>
	<div id="subscription_form" %>

		<% if current_user.subscribed?(@cposting)  %>			
			<%= render 'unsubscribe' %>
		<% else %>			
			<%= render 'subscribe' %>
		<% end %>

		

	</div>
<% end %>

